{
  "name": "wacz-seal",
  "version": "0.1.0",
  "description": "Offline, detached Ed25519 seals and Merkle inclusion proofs for WACZ web archives.",
  "type": "module",
  "module": "src/index.ts",
  "main": "src/index.ts",
  "bin": {
    "wacz-seal": "src/cli.ts"
  },
  "license": "MIT",
  "files": [
    "src",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "make-fixtures": "bun -e \"import { defaultWacz } from './src/fixtures.ts'; await Bun.write('fixtures/default.wacz', defaultWacz());\""
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "fflate": "0.8.2"
  }
}
